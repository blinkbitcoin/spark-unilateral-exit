node_modules/
coverage/
.direnv/
.env
*.log
e2e-output
.DS_Store

# Wallet material and recovery outputs -- these default filenames come from
# the Makefile, README, and runbook; never commit them.
*.seed
*seed*.txt
*.bundle.cleartext.json
recovery-bundle*.json
*.backup.json
recovery-packages*.json
sweeps*.json

# Checked-in fixtures are exempt.
!examples/*.json
!test/fixtures/*.json

# Webapp regtest captures reveal wallet graph metadata; keep them local.
test/fixtures/webapp/

# Persisted monitor scan state (blocks scanned, findings) - local only.
monitor-state.json
